name: Reusable Workflow

on:
  workflow_call:

jobs:
  reusable-job:
    runs-on: ubuntu-latest
    steps:
      - name: Reusable Workflow Started
        run: echo "✓ Reusable Workflow Started"

      - name: Processing
        run: echo "→ Processing reusable workflow logic"

      - name: Reusable Workflow Completed
        run: echo "✓ Reusable Workflow Completed"